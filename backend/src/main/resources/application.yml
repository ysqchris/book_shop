server:
  port: 8080

spring:
  application:
    name: used-book-platform
  
  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://11.142.154.110:3306/k60qsc1m?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: with_zmzjujkhpnghhayi
    password: "@ZCf92kZoGsX#z"

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

  # Jackson配置
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

# 本地上传目录（相对进程工作目录）
bookshop:
  upload:
    dir: uploads

# MyBatis Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# 日志配置
logging:
  level:
    com.bookshop: debug
    org.springframework.security: debug
